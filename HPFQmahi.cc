PriorityQueue<std::string, uint32_t> A();
PriorityQueue<QueuedPacket, uint32_t> C();
PriorityQueue<QueuedPacket, uint32_t> B();
if(p.class==Right){C.enq(p, getPrio(p,"C");
A.enq(p, getPrio(p,"A");
}
else{B.enq(p, getPrio(p,"B");
A.enq(p, getPrio(p,"A");
}
